# Code owners for FyAgent
#
# These mappings route review ownership. They become a merge gate only when
# GitHub branch protection or a ruleset explicitly requires Code Owner review.
# Without that live setting, the mappings are advisory and do not block merge.

# Global fallback for review routing.
*                 @farion1231

# Most sensitive paths — CI/signing/release and the Rust backend. Keep their
# review ownership explicit even if the global mapping changes later.
/.github/         @farion1231
/src-tauri/       @farion1231
