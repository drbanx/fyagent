# Implementation

1. Add classifier module/CLI, authoritative path map, and fixtures.
2. Add detect-changes, conditional domains, always-present required evaluator,
   and event-aware concurrency to CI.
3. Force full CI for dev pushes while preserving main push behavior.
4. Add pure dev-release eligibility module and exhaustive metadata fixtures.
5. Generalize release from fixed `v0.3.0` to strict canonical SemVer and exact
   dev-head push-CI evidence; keep dispatch non-publishing.
6. Integrate final NSIS/signing-state assets and retain multi-platform draft,
   digest, re-download, attestation, and Latest publication behavior.
7. Run workflow/unit/release-contract checks, action pin audits, and real
   dev-push/preflight/tag observations owned by the parent.
