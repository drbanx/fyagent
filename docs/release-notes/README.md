# Release Notes

这个目录不再保留已发布版本的版本化说明。历史发布事实以仓库 Git 历史和已发布的
GitHub Releases 页面为准；当前文档不会为了适配现行产品边界而改写既有发布记录。

未来每个正式版本必须在本目录新增与标签一致的英文说明：
`vX.Y.Z-en.md`。如需提供中文或日文版本，可同时新增
`vX.Y.Z-zh.md` 或 `vX.Y.Z-ja.md`，并在本索引中添加链接。
